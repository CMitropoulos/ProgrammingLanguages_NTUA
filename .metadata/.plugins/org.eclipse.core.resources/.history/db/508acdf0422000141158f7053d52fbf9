package CDIO.pathFinder.tests;

public class Bats {

	/**
	 * @param args
	 */
	public static void main(String[] args) {
		// TODO Auto-generated method stub
		int mapWidth = 0, mapHeight = 0, nbElements = 0;
		int x[];
		// read arguments
		mapWidth = Integer.parseInt(args[0].trim());
		mapHeight = Integer.parseInt(args[1].trim());
		nbElements = Integer.parseInt(args[2].trim());
		for(int i=0;i<nbElements;i++){
			args[i+3]=Integer.parseInt(x[i].trim());
			args
		}
	}

}
